{
    "name": "@flybyme/mesh-demos",
    "version": "0.1.0",
    "description": "Several parts in one repository: what the framework looks like when it is used rather than proved.",
    "type": "module",
    "private": true,
    "scripts": {
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "test:browser": "vitest run --config vitest.browser.config.ts --no-file-parallelism",
        "test": "npm run typecheck && npm run test:browser"
    },
    "dependencies": {
        "zod": "^3.25.76"
    },
    "devDependencies": {
        "@flybyme/mesh-core": "file:../mesh-core",
        "@flybyme/mesh-serve": "file:../mesh-serve",
        "@flybyme/mesh-web": "github:FLYBYME/mesh-web#v0.17.2",
        "@vitest/browser": "^2.1.9",
        "playwright": "^1.62.1",
        "typescript": "^5.9.3",
        "vitest": "^2.1.9"
    }
}

